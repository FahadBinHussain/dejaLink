javascript:(function(){function getDomain(url){url=url.replace(/(^\w+:|^)\/\//,'');url=url.replace(/^www\./,'');let parts=url.split('/')[0].split('.');return parts.slice(-2).join('.');}let elements=document.querySelectorAll('a');let youtubeLinks=new Set();elements.forEach(element=>{let originalText=element.href.trim().toLowerCase();let domain=getDomain(originalText);if(domain==='youtube.com'||domain==='youtu.be'){youtubeLinks.add(originalText);}});document.body.innerHTML='';youtubeLinks.forEach(link=>{let a=document.createElement('a');a.href=link;a.textContent=link;a.target='_blank';let p=document.createElement('p');p.appendChild(a);document.body.appendChild(p);});alert('All unique YouTube links are now on the page.');})();